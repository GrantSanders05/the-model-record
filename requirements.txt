# The model, backtester and optimizer are PURE STDLIB — nothing here is needed
# to fetch data, run a backtest, optimize parameters, or generate picks.
#
# These two are required only for the optional Google Sheets writeback.
google-auth>=2.0
requests>=2.28
